Fa�a git clone da aplicação

Docker nginx + php-fpm + wordpress cli
    construir os containers
        docker-compose build
    iniciar os containers
        docker-compose up -d
    finalizar os containers
        docker-compose down
Para acessar o container wp-cli
    docker exec -ti wp-cli bash
        Para instalar wordpress em portugues Brasil
            wp core download --path=./ --locale=pt_BR


