Docker nginx + php-fpm + wordpress

Faça git clone da aplicação

certifique-se a porta  [80 | 443] não estejam ocupadas


Altere as configurações do .env-example
cp .env-example .env

construir os containers
    docker-compose build
iniciar os containers
    docker-compose up -d
finalizar os containers
    docker-compose down

Para acessar o site local
    https://0.0.0.0/


