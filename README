Docker nginx + php-fpm + wordpress cli

Faça git clone da aplicação

Altere as configurações do .env-example
cp .env-example .env

construir os containers
    docker-compose build
iniciar os containers
    docker-compose up -d
finalizar os containers
    docker-compose down



